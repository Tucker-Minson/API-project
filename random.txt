--get current user--
    -need to include user firstname and lastname

-- get Details of Spot by Id --
    error message of :"avgRating[0].toJSON(...).avgRating.toFixed is not a function",

-- Log in a User --
successful response:
    -- remove createdAt & updatedAt
    --add token:"" column
